/target
/data/*
!/data/.gitkeep
/out/
/web/node_modules/
# Built by `npm run build` and embedded by kvad-serve; the directory itself has
# to exist for rust-embed to compile, which is what the .gitkeep is for.
/web/dist/*
!/web/dist/.gitkeep
