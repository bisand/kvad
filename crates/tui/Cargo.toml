[package]
name = "llm-tui"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Terminal UI for browsing, downloading and chatting with local models."

[[bin]]
name = "llm-tui"
path = "src/main.rs"

[dependencies]
llm = { path = "../llm" }
llm-gpu = { version = "0.1.0", path = "../gpu" }
ratatui = "0.29.0"
